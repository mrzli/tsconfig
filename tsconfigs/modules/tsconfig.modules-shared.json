{
  "extends": ["./tsconfig.modules-base.json"],
  "compilerOptions": {
    "module": "CommonJS", // affects compilation output, 'CommonJS' is still best for backend, there some peculiarities when using ES modules in Node
    "moduleResolution": "Node" // specifies how module files are located
  }
}
